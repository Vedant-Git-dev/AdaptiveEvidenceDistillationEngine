basically a project which uses langgraph to implement and manage multiple agents and coordination between them. System adapts the workflow dynamically according to current system state and optimize the context size. It uses cheap/free models to compress the context so frontier model only sees minimal context, reducing token cost
